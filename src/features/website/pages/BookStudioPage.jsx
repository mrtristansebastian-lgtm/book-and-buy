import { WebsiteSurfaceStudio } from '../components/WebsiteSurfaceStudio';

export function BookStudioPage() {
  return (
    <WebsiteSurfaceStudio
      surface="book"
      title="Book page"
      lede="Edit the public Book page — services, booking flow, and page copy."
    />
  );
}
